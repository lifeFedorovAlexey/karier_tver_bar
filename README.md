# Карьер — кафе и баня у воды

SSR-ready сайт на Next.js App Router. Публичные страницы: главная, кафе, баня и контакты. Страницы отдаются как готовый HTML и не требуют JavaScript для основного контента и навигации.

## Запуск

Требуется Node.js 22.12+.

```bash
npm install
npm run dev
```

Полная проверка:

```bash
npm run check
```

## Настройка перед публикацией

1. Скопировать `.env.example` в `.env.local`.
2. Указать боевой URL в `NEXT_PUBLIC_SITE_URL`.
3. После создания счётчика добавить `NEXT_PUBLIC_YANDEX_METRIKA_ID`. Без ID Метрика не загружается.
4. Подтвердить телефон, почту, режим работы, геоточку и позиции меню в `lib/site.ts` и страницах.
5. Подключить обработчик формы вместо временного `mailto:`.

## Фотографии

- `public/images/source/vk/*.jpg` — восемь исходных рекламных материалов, предоставленных владельцем проекта из сообщества VK.
- `public/images/karier-real-*.png` — очищенные от рекламных плашек версии трёх реальных сцен: общий вид комплекса, зал кафе и банный чан. Архитектура и исходные сюжеты сохранены; закрытые типографикой области восстановлены средствами OpenAI ImageGen.
- `public/images/source/vk-karier-01.jpg` — исходник, полученный ранее по прямой CDN-ссылке VK.

Перед коммерческой публикацией подтвердите права и предпочтительную подпись фотографий у владельца сообщества.
